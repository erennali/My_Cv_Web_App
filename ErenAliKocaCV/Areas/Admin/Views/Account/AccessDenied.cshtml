@{
    ViewData["Title"] = "Erişim Reddedildi";
    Layout = "_AdminLayout";
}

<div class="container-fluid px-4 py-5">
    <div class="row justify-content-center">
        <div class="col-lg-7">
            <div class="card shadow-lg border-0 rounded-lg mt-5">
                <div class="card-header bg-danger text-white">
                    <h3 class="text-center font-weight-light my-2">
                        <i class="fas fa-exclamation-triangle me-2"></i>Erişim Reddedildi
                    </h3>
                </div>
                <div class="card-body text-center py-5">
                    <div class="mb-4">
                        <i class="fas fa-lock fa-6x text-danger mb-4"></i>
                        <h2 class="mb-3">403 - Yetkisiz Erişim</h2>
                        <p class="lead">Bu sayfaya erişim için gerekli izinlere sahip değilsiniz.</p>
                        <p>Erişim için yönetici hesabıyla giriş yapmanız gerekmektedir.</p>
                    </div>
                    <div class="mt-4">
                        <a href="@Url.Action("Index", "Dashboard", new { area = "Admin" })" class="btn btn-primary me-2">
                            <i class="fas fa-home me-1"></i> Yönetici Ana Sayfası
                        </a>
                        <a href="@Url.Action("Login", "Account", new { area = "Admin" })" class="btn btn-success me-2">
                            <i class="fas fa-sign-in-alt me-1"></i> Giriş Yap
                        </a>
                        <a href="javascript:history.back()" class="btn btn-secondary">
                            <i class="fas fa-arrow-left me-1"></i> Geri Dön
                        </a>
                    </div>
                </div>
                <div class="card-footer text-center py-3">
                    <div class="small">
                        Eğer bu bir hata olduğunu düşünüyorsanız, lütfen site yöneticisiyle iletişime geçin.
                    </div>
                </div>
            </div>
        </div>
    </div>
</div> 